# react-toy-calendar
experiments with react by building an interactive calendar

A small react calendar. 
One year ahead, starting from today. 

Uses webpack, react, ramda and moment.js.

## installation:

```
npm install
```

## run
serve with `webpack-dev-server `



